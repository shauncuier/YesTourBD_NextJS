Square icon-only button for toolbars, card corners, and compact actions (close, favorite, share, WhatsApp/call shortcuts).

```jsx
<IconButton icon={<Icon name="heart" size={18} />} aria-label="Save to wishlist" variant="outline" />
```

Always pass `aria-label`; there is no text fallback.
