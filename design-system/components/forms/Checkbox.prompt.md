Single boolean choice — add-ons, "remember me", terms acceptance, filter facets in a listing sidebar.

```jsx
<Checkbox label="Include breakfast" checked={hasBreakfast} onChange={(e) => setHasBreakfast(e.target.checked)} />
```
