Boolean toggle for instant settings — "Available now only", SMS notifications, dark mode.

```jsx
<Switch label="Show instant-booking only" checked={instantOnly} onChange={(_, v) => setInstantOnly(v)} />
```
