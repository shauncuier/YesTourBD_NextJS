Transient notice for the result of an action — booking placed, payment failed, request submitted. Stack bottom-right with 12px gaps; the host owns positioning and timers.

```jsx
<Toast tone="success" icon={<Icon name="check-circle" size={18}/>}
  title="Request submitted" message="Our team will send your quotation within 2 hours."
  onClose={dismiss} />
```

White surface with a 4px tone-coloured left bar — the tone never floods the background.
