Names an icon-only control or expands a truncated value. Shows on hover and focus; a few words only — put anything longer in the UI itself.

```jsx
<Tooltip label="Add to wishlist"><IconButton icon={<Icon name="heart" size={18}/>} aria-label="Add to wishlist" /></Tooltip>
```

Navy-900 chip, 8px off the trigger, no arrow.
